For very brief information on how to install and use the
program, type `when --help' at the command line, or read
the first page of the source code, in the file `when'. For
more detailed information, install the software and do
`man when'.
